<div class="row">
    <div class="process">
        <div class="process-row nav nav-tabs">
            <div class="process-step">
                <button type="button" class="btn  {% if selected == 1  %}btn-info {% else  %} btn-default {% endif %}  btn-circle" data-toggle="tab" href="#menu1"><i class="fa fa-info fa-3x"></i></button>
                <p class="steps"><small>Pasiūlymai</small></p>
            </div>
            <div class="process-step">
                <button type="button" class="btn {% if selected == 2  %}btn-info {% else  %} btn-default {% endif %}  btn-circle" data-toggle="tab" href="#menu2"><i class="fa fa-file-text-o fa-3x"></i></button>
                <p class="steps"><small>Užsakymo informacija</small></p>
            </div>
            <div class="process-step">
                <button type="button" class="btn {% if selected == 3  %}btn-info {% else  %} btn-default {% endif %}  btn-circle" data-toggle="tab" href="#menu6"><i class="fa fa-check fa-3x"></i></button>
                <p class="steps"><small>Patvirtinimas</small></p>
            </div>
        </div>
    </div>
</div>